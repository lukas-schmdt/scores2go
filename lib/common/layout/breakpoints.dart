/// Width at which the layout switches from mobile (bottom nav) to
/// tablet/laptop (navigation rail).
const double kMobileBreakpoint = 600.0;

/// Width at which the navigation rail expands to show labels inline.
const double kWideBreakpoint = 1200.0;
