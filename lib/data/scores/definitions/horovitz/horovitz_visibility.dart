import 'package:scores_2_go/model/score.dart';
import 'package:scores_2_go/model/score_visibility.dart';

// Both variables are always active.
ScoreVisibility horovitzVisibility(Score score) => const ScoreVisibility.all();
