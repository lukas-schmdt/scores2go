import 'package:scores_2_go/model/score.dart';
import 'package:scores_2_go/model/score_visibility.dart';

// All Finnegan variables are always active.
ScoreVisibility finneganVisibility(Score score) => const ScoreVisibility.all();
